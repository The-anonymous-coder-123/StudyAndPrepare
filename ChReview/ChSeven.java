import java.util.*;
public class ChSeven  {
  public static void main(String[] args)  {
    //Arrays. in this chapter, the [] brackets will be used, so the predetermined values/variables will be surrounded by the # symbol.
      //Constructor syntax
    <type>[] <name> = new <type>[<value>]; //notice that I put a value spot on the right. if this is not present, the array will not work, as it determines the size of the array.
    //more [] pairs give another dimension to the array
    <type>[][] <name> = new <type>[<value>][<value>]; //two dimensions. value^2 possible combinations
    //there is almost no limit the amount of dimensions in an array, but 4 is usually the logical max.
    //once the array is created, the array varaibles auto-initialize to a zero-equivalent value.
    
    /* Zero equivalents
    type      value
    int         0
    double     0.0
    char      '\0'
    boolean     false
    objects     null <-- see Main.java, under Terminology
                    

                      */
  


  }
}